Implement SLICE-12 "OTA Manual and Integration Hardening" for Hotel-Chipre-PMS (FastAPI + SQLAlchemy 2; SQLite tests). You are already in the integration trunk worktree — do NOT merge or switch branches. Read docs/v72-vertical-slices-plan.md (SLICE-12) and docs/business-requirements-master-v72.md section 10 (OTA manual y duplicados), 13.2 (OTA sin pago).

Context already present: OTA models (app/models/ota.py, ota_core.py), app/services/ota_service.py, app/api/ota_webhooks.py, app/services/reservation_action_service.py, reservations with source_provider_code + external_id + uq_reservation_ota_external_id dedup constraint. Permission matrix + require_permission exist. NO migration expected (dedup constraint already exists).

Implement:
- app/services/ota_manual_service.py: create_or_update_manual_ota_reservation (requires channel + external_id; if (hotel_id, source_provider_code, external_id) already exists, UPDATE the existing reservation and write an audit entry to SecurityAuditLog instead of creating a duplicate — honor the unique constraint). release_no_guarantee(reservation): internal release for OTA reservations without payment guarantee that does NOT call any provider cancel API (internal-only state change, audited).
- app/schemas/ota_manual.py.
- Wire into app/api/reservations.py: a manual-OTA create/update endpoint gated by require_permission(reservation:create). Keep edits minimal.
- Webhook hardening in app/api/ota_webhooks.py: ensure deprecated webhook routes return HTTP 410 (verify/keep existing behavior); never trust caller-provided hotel_id without the per-hotel webhook secret.

Tests (tests/test_ota_manual_service.py): test_manual_ota_requires_channel_and_external_id, test_duplicate_channel_external_id_updates_existing_reservation_and_audits, test_no_guarantee_ota_internal_release_does_not_call_provider_cancel, test_deprecated_ota_webhook_routes_return_410, cross-hotel isolation. TEST-FIXTURE RULE: db.flush() after adding HotelConfiguration before children (PRAGMA foreign_keys=ON). Keep existing OTA tests green.

Do NOT modify payment, cash, company, guest, room-block, allocation, laundry, stock, whatsapp, public_booking files. You MAY edit reservations.py, ota_webhooks.py, ota_service.py, reservation_action_service.py, permission_service.py (append codes if needed), main.py.

Evidence: `python -m pytest tests/ --ignore=tests/test_postgres_validation.py -q` before and after; trunk baseline 479 passed, 8 skipped, 0 failures; keep 0 failures + new tests. `python -c "import app.models; import app.main"` passes. Commit with git if possible, else leave edits. End with "INTEGRATION EDITS" listing exact edits to app/main.py, app/api/reservations.py, app/api/ota_webhooks.py, app/services/ota_service.py, app/services/permission_service.py.
