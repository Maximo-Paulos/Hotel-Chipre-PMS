Implement SLICE-08 "Cash Register and Arqueo" for Hotel-Chipre-PMS (FastAPI + SQLAlchemy 2; SQLite tests, PostgreSQL prod). You are already in the integration trunk worktree — do NOT merge or switch branches. Read docs/v72-vertical-slices-plan.md (SLICE-08), docs/data-foundations/transactions-vs-payments.md, docs/business-requirements-master-v72.md section 17 (caja).

Context already present: cash_sessions, cash_movements, cash_close_reports tables + app/models/cash_register.py (CashSession, CashMovement, CashCloseReport, CashSessionStatusEnum, CashMovementTypeEnum) EXIST. Permission matrix + require_permission exist. transactions is the canonical ledger.

Implement:
- app/services/cash_register_service.py: open_session (enforce AT MOST ONE open session per hotel — raise CashRegisterError if one is already open), add_movement (income/expense/adjustment; only into an open session), close_session producing a CashCloseReport whose expected_balance = opening_balance + sum of confirmed cash movements in the session window; record counted vs expected difference; if difference != 0 the close stays pending_approval until approved by a permitted user. Use SELECT ... with_for_update() on the session row in close to avoid races. All hotel_id-scoped.
- app/api/cash_register.py: open / close / add-movement / list endpoints. Add permission codes "cash:operate" (owner True, co_owner True, manager True, receptionist True, housekeeping False) and "cash:approve_difference" (owner True, co_owner True, manager True, receptionist False, housekeeping False) to app/services/permission_service.py. Register router in app/main.py.
- If you enforce one-open-session at the DB level, add a migration (down_revision="20260613_laundry_stock") with a PostgreSQL partial unique index on cash_sessions(hotel_id) WHERE status='open' plus a SQLite-compatible sqlite_where fallback; explicit names; cross-dialect. Otherwise enforce in the service only and say so.
- tests/test_cash_register_service.py + tests/test_cash_register_api.py: test_only_one_open_cash_session_per_hotel_is_allowed, test_cash_movement_requires_open_session, test_close_report_expected_balance_from_confirmed_cash, test_close_with_difference_requires_approval, cross-hotel isolation. TEST-FIXTURE RULE: db.flush() after adding HotelConfiguration before children (PRAGMA foreign_keys=ON).

Do NOT modify room-block, laundry, stock, company, guest, or checkin files. You MAY append permission codes to permission_service.py, add a migration, edit main.py, and make a minimal hook in payment_service.py only if cash payments must create a movement.

Evidence: `python -m pytest tests/ --ignore=tests/test_postgres_validation.py -q` before and after. Trunk baseline 425 passed, 8 skipped, 0 failures. Keep 0 failures + your new tests. `python -c "import app.models; import app.main"` passes. If you added a migration, `python -m alembic heads` shows exactly one head. Commit with git if possible, else leave edits in the working tree. End by printing "INTEGRATION EDITS" with exact edits to app/main.py, app/services/permission_service.py, and any migration filename.
