Implement SLICE-03 "Corporate Reservations and Documents" for Hotel-Chipre-PMS (FastAPI + SQLAlchemy 2; SQLite tests). You are already in the integration trunk worktree — do NOT merge or switch branches. Read docs/v72-vertical-slices-plan.md (SLICE-03) and docs/business-requirements-master-v72.md section 3 (Reservas de empresa).

Context already present: companies + company_documents tables and app/models/company.py (Company) + app/models/company_document.py (CompanyDocument, CompanyDocumentTypeEnum, CompanyDocumentStatusEnum) EXIST. Company commercial fields exist (base_price, payment_deferred, deferred_days, requires_voucher, requires_signature). The permission matrix exists: from app.services.permission_service import resolve and the PERMISSION_* codes; from app.dependencies.auth import require_permission. SecurityAuditLog exists (app/models/security_audit_log.py). reservations.company_id FK exists. So you need NO new migration.

Implement:
- app/services/company_document_service.py: create_document, set_signature_status (pending -> signed/rejected lifecycle, invalid transitions raise CompanyDocumentError), list_documents_for_company, list_documents_for_reservation. Hotel-scoped; audit signature changes to SecurityAuditLog.
- app/schemas/company_document.py: create/read/status pydantic schemas (from_attributes=True).
- app/api/company_documents.py: CRUD + status-transition endpoints, gated by require_permission. Add a permission code "company:manage" to app/services/permission_service.py: add to PERMISSION_DEFINITIONS and to DEFAULT_MATRIX for every role (owner True, co_owner True, manager True, receptionist False, housekeeping False). Register the router in app/main.py (import in the api import block and app.include_router(company_documents.router)).
- Corporate reservation rule: inspect app/models/reservation.py and app/services/allocation_engine.py to find the allocation-lock signal the solver respects (e.g. an allocation_locked flag or ReservationAllocationLock). In app/services/reservation_service.py reservation-creation path, when a reservation has company_id and that company has requires_signature OR payment_deferred set, mark it allocation-locked, and apply company.base_price as the default rate when no explicit rate is given (still overridable). Keep edits minimal and guarded so non-corporate reservations are unaffected.
- tests/test_company_documents_service.py + tests/test_company_documents_api.py + a corporate-reservation test: test_company_document_signature_status_flow, test_receptionist_cannot_manage_company_by_default, test_company_base_price_applies_as_reservation_default_but_overridable, test_corporate_reservation_is_allocation_locked, plus cross-hotel isolation. TEST-FIXTURE RULE: when a test seeds HotelConfiguration then child rows that carry hotel_id as a plain integer FK, call db.flush() right after adding the HotelConfiguration and before adding children (tests run with PRAGMA foreign_keys=ON; SQLAlchemy does not order the parent insert first for plain-integer FKs).

Do NOT modify payment, cash, room-block, laundry, stock, guest-tag, or checkin files. You MAY append a permission code to permission_service.py and edit companies.py + reservation_service.py (corporate path only) + main.py.

Evidence: run `python -m pytest tests/ --ignore=tests/test_postgres_validation.py -q` before and after. The trunk baseline is 425 passed, 8 skipped, 0 failures. You must keep 0 failures and add your new tests passing. Run `python -c "import app.models; import app.main"` to prove imports. Commit your work with git if possible; if git commit fails due to sandbox permissions, just leave the edited files in the working tree. End by printing a section "INTEGRATION EDITS" listing the exact lines you added to app/main.py, app/services/permission_service.py, app/services/reservation_service.py, and app/api/companies.py.
