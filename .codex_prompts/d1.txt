Add Room Blocks management UI to the existing Rooms page in Hotel-Chipre-PMS frontend (React 18 + TS + Vite 5 + TanStack Query v5). You are in the integration trunk worktree; work in frontend/.

Study conventions first: frontend/src/api/client.ts, frontend/src/api/rooms.ts, frontend/src/views/protected/RoomsPage.tsx (the page you will extend), and an existing page that lists+mutates (GuestsPage.tsx). Match style + Spanish labels. Derive the contract from backend app/api/room_blocks.py.

Implement:
- frontend/src/api/roomBlocks.ts (NEW): list active blocks, create block (date-ranged or indefinite, reason), resolve block. Use the existing client wrapper.
- Extend frontend/src/views/protected/RoomsPage.tsx: add a "Bloqueos" section/tab showing active room blocks with create (room, date range or indefinite, reason) and resolve actions, using TanStack Query mutations that invalidate the relevant queries.
ONLY edit RoomsPage.tsx and add roomBlocks.ts (+ a hook file if that matches the pattern). Do NOT touch ReservationsPage, GuestsPage, router.tsx, AppShell.tsx, or other api modules.

Evidence: `cd frontend && npm run build` MUST succeed. Commit with git if possible, else leave edits in the working tree. End by printing "INTEGRATION EDITS" naming the files you created/modified.
