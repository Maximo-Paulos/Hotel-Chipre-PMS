Implement SLICE-02 "Guest Profile, Tags, and Check-in Override" for Hotel-Chipre-PMS (FastAPI + SQLAlchemy 2; SQLite tests). You are already in the integration trunk worktree — do NOT merge or switch branches. Read docs/v72-vertical-slices-plan.md (SLICE-02) and docs/business-requirements-master-v72.md sections 2 (Huespedes) and 7.

Context already present: guests + guest_tags tables and app/models/guest.py (Guest, GuestTag, GuestRatingEnum, GuestTagTypeEnum incl PROHIBIDO_ALOJAR) EXIST. app/services/checkin_service.py ALREADY blocks check-in when the guest has an active prohibido_alojar tag (raises CheckInError). The permission matrix exists with code "guest:tags" already defined in app/services/permission_service.py DEFAULT_MATRIX. require_permission + SecurityAuditLog exist. So you need NO new migration.

Implement:
- app/services/guest_service.py: search_guests (by document, phone, email, last_name — hotel-scoped), quick_profile (guest + last 5 stays + active tags), add_tag / resolve_tag (set expires_at or a resolved flag), set_rating. Audit tag/rating changes to SecurityAuditLog.
- app/schemas/guest_tag.py: create/read schemas for tags.
- app/api/guests.py: add endpoints for guest tags (list/add/resolve) gated by require_permission("guest:tags"), and a search endpoint. Do NOT change the existing update_guest gating (already require_permission(PERMISSION_GUEST_EDIT)).
- CHECK-IN OVERRIDE (key V72 rule): extend app/services/checkin_service.py and app/api/checkin.py so an authorized manager can OVERRIDE a prohibido_alojar block. Add a permission code "checkin:override_prohibido" to permission_service.py (owner True, co_owner True, manager True, receptionist False, housekeeping False). The checkin endpoint should accept an optional override flag; when set, require that permission and, if granted, allow check-in despite the tag while writing a SecurityAuditLog entry (action "checkin.prohibido_override") recording who overrode and the tag note. Without the flag, the existing block stands.
- tests/test_guest_service.py + extend check-in tests: test_guest_search_matches_document_phone_email_name, test_guest_quick_profile_returns_recent_stays_and_tags, test_prohibido_alojar_blocks_checkin_without_override, test_authorized_override_allows_checkin_and_audits, test_unauthorized_role_cannot_override. Keep the existing tests/test_checkin_security.py passing. TEST-FIXTURE RULE: db.flush() after adding HotelConfiguration before children (PRAGMA foreign_keys=ON).

Do NOT modify payment, cash, room-block, laundry, stock, or company files. You MAY append a permission code to permission_service.py and edit guests.py, checkin.py, checkin_service.py, main.py.

Evidence: `python -m pytest tests/ --ignore=tests/test_postgres_validation.py -q` before and after. Trunk baseline 425 passed, 8 skipped, 0 failures. Keep 0 failures + your new tests, and tests/test_checkin_security.py must stay green. `python -c "import app.models; import app.main"` passes. Commit with git if possible, else leave edits in the working tree. End by printing "INTEGRATION EDITS" with exact edits to app/main.py, app/services/permission_service.py, app/services/checkin_service.py, app/api/checkin.py, app/api/guests.py.
