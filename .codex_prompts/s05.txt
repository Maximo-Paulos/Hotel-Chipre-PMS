Implement SLICE-05 "Allocation Engine V72 Rules" for Hotel-Chipre-PMS (FastAPI + SQLAlchemy 2; SQLite tests). You are already in the integration trunk worktree — do NOT merge or switch branches. Read docs/v72-vertical-slices-plan.md (SLICE-05) and docs/business-requirements-master-v72.md sections 5 (Motor de reservas), 6 (Restriccion motriz), 8.2/8.6.

Context already present: room_movement_groups table + RoomMovementGroup model and RoomMoveEvent.movement_group_id EXIST. app/services/allocation_engine.py is the solver; it already excludes blocked rooms (SLICE-04). app/models/reservation.py has mobility_restriction, version (optimistic lock), and an allocation-lock signal; rooms have score + is_accessible. Permission matrix + require_permission exist. Likely NO migration; if you add one, down_revision="20260613_cash_open_idx", explicit names, cross-dialect.

Implement the V72 hard rules in app/services/allocation_engine.py (and allocation_runtime_service.py if used):
- Mobility-restricted guests (reservation.mobility_restriction) must prefer the LOWEST compatible floor and only accessible rooms; treat as a hard constraint, not a tiebreaker.
- rooms.score is a TIEBREAKER ONLY (applied after all hard constraints; never overrides availability/compatibility).
- The solver must NOT move reservations that are protected: checked-in / check-in started / pre_check_in, allocation_locked, corporate (company with requires_signature or payment_deferred), or manually-pinned. Extend the lock predicate (e.g. is_locked / ReservationSlot) accordingly.
- Movement groups: when an allocation run moves multiple rooms for one cause, create a RoomMovementGroup and link the RoomMoveEvents to it.
- Audited group revert: app/services/room_movement_group_service.py (NEW) with revert_group(group_id) that reverts all linked moves and marks affected reservations protected; audit to SecurityAuditLog. Expose via app/api/room_movement_groups.py (NEW) gated by require_permission (reuse "reservation:room_move" or add "allocation:revert"; if you add a code, append it to permission_service.py for owner/co_owner/manager True, others False). Register router in app/main.py.
- app/schemas/room_movement_group.py (NEW).

Tests (tests/test_allocation_v72_rules.py + tests/test_room_movement_groups_api.py): test_mobility_restriction_prefers_lowest_compatible_floor, test_score_only_breaks_equivalent_room_tie, test_solver_does_not_move_corporate_manual_or_pre_checkin_reservations, test_allocation_run_creates_movement_group_and_events, test_revert_movement_group_marks_reservations_protected, cross-hotel isolation. TEST-FIXTURE RULE: db.flush() after adding HotelConfiguration before children (PRAGMA foreign_keys=ON). Keep existing tests/test_allocation_engine.py green.

Do NOT modify payment, cash, company, guest, room-block, laundry, stock files. You MAY edit allocation_engine.py, allocation_runtime_service.py, reservation_operations_service.py, operations.py (models), reservations.py, permission_service.py, main.py.

Evidence: `python -m pytest tests/ --ignore=tests/test_postgres_validation.py -q` before and after; trunk baseline 457 passed, 8 skipped, 0 failures; keep 0 failures + new tests. `python -c "import app.models; import app.main"` passes. If you added a migration, `python -m alembic heads` shows one head. Commit with git if possible, else leave edits in the working tree. End with "INTEGRATION EDITS" listing exact edits to app/main.py, app/services/permission_service.py, app/services/allocation_engine.py, app/services/allocation_runtime_service.py, app/services/reservation_operations_service.py, app/models/operations.py, app/api/reservations.py.
