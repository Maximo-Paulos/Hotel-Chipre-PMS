Implement SLICE-04 "Room Blocks, Availability, Housekeeping" for Hotel-Chipre-PMS (FastAPI + SQLAlchemy 2; SQLite tests, PostgreSQL prod). You are already in the integration trunk worktree — do NOT merge or switch branches. Read docs/v72-vertical-slices-plan.md (SLICE-04) and docs/business-requirements-master-v72.md sections 4, 13, 14.

Context already present: the room_blocks table + app/models/room_block.py (RoomBlock, RoomBlockReasonEnum) EXIST. Permission system exists: from app.services.permission_service import resolve and PERMISSION_* codes; from app.dependencies.auth import require_permission. So you need NO new migration unless you add genuinely new schema (if so, down_revision="20260613_laundry_stock", explicit constraint names, cross-dialect).

Implement:
- app/services/room_block_service.py: create_block (date-ranged or indefinite; reject overlap with a protected/checked-in reservation by raising a typed RoomBlockError the caller maps to manual review), list_active_blocks(hotel_id, date_range), resolve_block (set resolved_at). All hotel_id-scoped.
- app/api/room_blocks.py: CRUD endpoints. Gate create/resolve behind require_permission. Add a permission code "room:block" to app/services/permission_service.py PERMISSION_DEFINITIONS + DEFAULT_MATRIX (owner True, co_owner True, manager True, receptionist False, housekeeping False). Register router in app/main.py.
- AVAILABILITY INTEGRATION (the core value): in app/services/reservation_service.py, make check_room_availability and find_available_rooms EXCLUDE rooms that have an active room_block overlapping the requested dates. Centralize the exclusion in one helper and reuse it. If app/services/allocation_engine.py builds candidate rooms (build_slots_from_db or similar), exclude blocked rooms there too so availability and allocation agree.
- tests/test_room_block_service.py + tests/test_room_blocks_api.py: test_active_room_block_excludes_room_from_availability, test_indefinite_block_excludes_future_dates_until_resolved, test_block_overlapping_protected_reservation_requires_manual_resolution, test_resolved_block_frees_room, cross-hotel isolation. TEST-FIXTURE RULE: when seeding HotelConfiguration then child rows with plain-integer hotel_id FKs, call db.flush() right after adding HotelConfiguration and before children (tests run PRAGMA foreign_keys=ON).

Do NOT modify payment, cash, laundry, stock, company, guest-tag, or checkin files. You MAY append a permission code to permission_service.py and edit reservation_service.py + allocation_engine.py + rooms.py + main.py.

Evidence: run `python -m pytest tests/ --ignore=tests/test_postgres_validation.py -q` before and after. Trunk baseline is 425 passed, 8 skipped, 0 failures. Keep 0 failures and add your new tests passing. `python -c "import app.models; import app.main"` must pass. If you added a migration, `python -m alembic heads` must show exactly one head. Commit with git if possible; otherwise leave edits in the working tree. End by printing "INTEGRATION EDITS" with the exact edits to app/main.py, app/services/permission_service.py, app/services/reservation_service.py, app/services/allocation_engine.py, app/api/rooms.py.
