{
  "watch": ["app"],
  "ext": "py",
  "ignore": ["frontend/**", "tests/**", ".venv/**"],
  "exec": "./.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8040 --reload",
  "env": {
    "DEMO_MODE": "false"
  },
  "legacyWatch": true
}
