# tests/AGENTS

- Treat tests as part of the implementation, not as an afterthought.
- Prefer deterministic coverage for business rules, security checks, and API contracts.
- Add regression tests for every bug fix or behavior change where practical.
- Keep fixtures realistic but minimal.
- Do not weaken assertions unless the product requirement changed.

